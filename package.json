{
  "name": "beefbrain",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "init-items": "node scripts/init-item-metadata.js",
    "dev": "pnpm --filter bnb-web dev"
  },
  "devDependencies": {
    "js-yaml": "^4.1.1"
  }
}
