{
  "name": "beefbrain",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "init-items": "node scripts/init-item-metadata.js"
  },
  "devDependencies": {
    "js-yaml": "^4.1.1"
  }
}
